# Development Rules

## 当前阶段

- v0.2 MVP 与 v0.3 知识库归档增强功能均已实现并完成基础验证。
- 后续 v0.3 修复和兼容性增强必须按 `tasks/plan.md` 的依赖顺序推进，并在每个检查点运行对应测试。

## 始终执行

- 所有输入路径、文件类型、文件大小和输出路径必须验证。
- 文件内容不得上传、打印到日志或写入版本控制。
- 为每个已支持格式维护无敏感信息的测试样本与预期 Markdown。
- 转换不能无声丢失内容；未知元素必须写入转换报告。
- 默认单文件输出协议必须兼容；Obsidian 元数据、来源链接与原文件复制必须显式启用。
- 批处理必须保持单项失败隔离，且 Tkinter 控件只能在主线程更新。
- PPT 视觉导出必须为显式选项，优先使用 WPS 演示并以 PowerPoint 为后备；以只读方式运行，不执行宏。失败时不能中断 Markdown 转换或保留失效视觉链接。

## 先确认

- 新增依赖、改变本地数据保留策略、支持新文件格式。
- 任何网络调用、遥测、云同步、账户或第三方服务。
- 覆盖已有输出目录或处理超过产品限制的大文件。
- 将批处理从顺序改为并发，或放宽路径/标签/YAML 校验规则。

## 禁止

- 提交输入文档、导出结果、临时文件、凭据或个人数据。
- 执行宏、嵌入脚本或文档中的外部链接。
- 将视觉还原或公式计算描述为已支持，除非有对应测试证据。
