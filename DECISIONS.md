# Decisions

## D-001：MVP 采用本地优先运行方式

- 日期：2026-09-01
- 状态：已接受
- 决定：文件只在本机读取、处理和输出，MVP 不提供云端上传或账户系统。
- 原因：用户的办公文件可能含有私人或敏感内容；个人使用场景不需要云端协作。
- 影响：部署和分发需要在 Windows 本机完成；后续若增加云端能力，必须重新进行安全与隐私设计。

## D-002：MVP 仅支持标准 OOXML 输入

- 日期：2026-09-01
- 状态：已接受
- 决定：首版支持 DOCX、PPTX、XLSX；WPS 原生格式和旧版二进制格式不在范围内。
- 原因：WPS 可以导出标准格式；先验证高频文件的核心价值，避免引入 WPS 自动化和兼容性风险。
- 影响：原生格式支持作为单独适配器和后续决策，不污染转换核心。

## D-003：输出以语义化 Markdown 为主

- 日期：2026-09-01
- 状态：已接受
- 决定：优先提取文本层级、表格、链接和图片；不能可靠表达的内容进入转换报告。
- 原因：知识库归档和 AI 阅读依赖可搜索、可引用的语义内容，而非原始视觉版式。
- 影响：产品必须对降级内容透明，不能暗示无损转换。

## D-004：采用 Windows 原生桌面 UI

- 日期：2026-09-01
- 状态：已接受
- 决定：MVP 使用 Python 标准库 Tkinter 原生桌面界面，直接调用转换应用服务。
- 原因：用户需要选择输出目录；纯浏览器界面不能跨浏览器可靠地获得任意本地目录写入权限。桌面界面也更适合离线文件处理与未来 Windows 安装包。
- 替代方案：PySide6 提供更丰富的桌面组件，但其运行时下载在当时多次中断，且 MVP 只需文件/目录选择与状态展示；本地 FastAPI + 浏览器界面也会受输出目录权限限制。
- 影响：确认后新增桌面依赖与打包链路；若改选浏览器方案，必须同时更新产品流程和输出交付方式。

## D-005：以 PySide6-Essentials 替换 Tkinter 分发宿主

- 日期：2026-09-01
- 状态：已取代（D-006）
- 决定：MVP 的 Windows 分发应用改用 `PySide6-Essentials` 的 Qt 原生桌面界面，直接调用转换应用服务。
- 原因：打包验证发现当前 Python 环境缺少可用 Tcl/Tk 资源，PyInstaller 会排除 `tkinter`，使 Tkinter 应用无法在分发产物中启动。`PySide6-Essentials` 包含所需的 Qt Widgets 运行时并可随应用分发。
- 替代方案：修复或重建 Tcl/Tk 环境将把分发正确性依赖于当前构建机的 Python 安装；本地 FastAPI + 浏览器界面仍受目录写入权限限制；Tauri + React + Python sidecar 增加 Rust、TypeScript 与进程通信复杂度。
- 影响：增加约 77 MB 的 Qt Essentials 分发依赖；转换核心保持 UI 无关。每次发布必须构建并启动新产物，确认桌面运行时已被包含。

## D-006：以完整 Tcl/Tk 的 Python 3.13 构建 Tkinter 分发应用

- 日期：2026-09-01
- 状态：已接受
- 决定：MVP 恢复使用 Tkinter 桌面 UI，并使用项目内 `.venv-tk`（基于 Python 3.13.9、Tcl/Tk 8.6）执行测试和 PyInstaller 构建。
- 原因：Qt Essentials 在当前 PyInstaller 运行时出现不可解析的 Windows 原生 DLL 链，即使文件收集与路径补偿均已尝试，分发产物仍无法加载 `QtWidgets`。系统 Python 3.13 已验证包含可用 Tcl/Tk；其 PyInstaller 产物正确收集 Tcl/Tk 数据并成功创建主窗口。
- 替代方案：继续修补 Qt DLL 布局风险高且难以在当前环境可靠验证；浏览器和 Tauri 方案仍不符合 MVP 的本地目录选择与复杂度边界。
- 影响：Windows 构建要求使用 `.venv-tk`，不能用缺失 Tcl/Tk 的旧 `.venv`。转换核心保持 UI 无关；后续升级 Python 时必须重新执行完整构建与主窗口验证。

## D-008：分发构建使用可验证的 Python 3.12 环境与 ASCII 内部产物名

- 日期：2026-09-01
- 状态：已接受，取代 D-006 的构建环境部分。
- 决定：Windows 分发构建使用项目 `.venv`（Python 3.12.13）；PyInstaller 的内部 `--name` 固定为 ASCII 的 `OfficeToMarkdown`，构建成功后再由 PowerShell 在文件系统层将目录与 exe 重命名为“廾匸转换”。
- 原因：`.venv-tk` 已无法启动解释器；Windows PowerShell 向原生命令传递中文 `--name` 参数时会产生乱码产物。当前 `.venv` 已通过完整构建、运行时文件检查和主窗口验证。
- 影响：构建脚本不能将中文发布名直接传给 PyInstaller；每次发布仍需验证最终中文 exe 与主窗口标题。

## D-009：按源文件名分离存放 Markdown 与转换报告

- 日期：2026-09-01
- 状态：已接受并已实施。
- 决定：每次转换在输出根目录下创建 `markdown/` 与 `reports/`。DOCX、PPTX 与 XLSX 都生成 `markdown/<源文件名>.md`；XLSX 另保留 `markdown/sheets/` 的逐工作表文件。报告命名为 `reports/<源文件名>转换报告.md`。
- 原因：固定的 `content.md`、`slides.md` 与 `conversion-report.md` 在批量知识库归档中不便识别，且正文和报告混在同一层级。
- 影响：`index.md`、报告打开操作和 `ConversionResult.report_path` 全部指向新位置；既有输出目录保持原样，不自动迁移。

## D-010：PPTX 视觉预览采用本机 PowerPoint 的可选自动化导出

- 日期：2026-09-01
- 状态：已接受并已实施。
- 决定：用户可分别勾选“PPT 导出每页 PNG”和“PPT 导出版式 PDF”。系统通过 Microsoft PowerPoint COM 以只读、禁用宏的方式导出到 `visuals/`；导出 PowerShell 强制以 STA 模式运行。PNG 位于 `visuals/pages/`，PDF 位于 `visuals/<文件名>.pdf`。
- 原因：Markdown 无法表达页面视觉关系，而 PowerPoint 原生渲染比自行复刻版式可靠；将其设为可选避免为所有转换引入额外时间、存储与本机软件依赖。
- 替代方案：LibreOffice 可减少 Microsoft Office 依赖，但本机未检测到可用安装且复杂 PPT 的视觉兼容性较低；截图/OCR 无法保证版式保真。
- 影响：仅 PPTX 且显式启用时调用本机 PowerPoint；导出失败会清理未完成视觉目录、移除失效索引链接并记录通用警告，Markdown 主流程不失败。仍要求运行该桌面应用的 Windows 会话具有可用的交互式 PowerPoint 自动化权限。

## D-011：PPTX 视觉导出改为 WPS 优先、PowerPoint 后备

- 日期：2026-09-02
- 状态：已接受并已实施。
- 决定：PPTX 视觉导出首先调用 WPS 演示 COM；WPS 以只读方式打开标准 PPTX，使用 `SaveAs(..., 32)` 导出 PDF，并逐页导出 PNG。WPS 不可用或导出失败时，才调用既有 PowerPoint 导出器。
- 原因：本机已实测 WPS 演示自动化可生成 PDF 和 PNG，且用户工作流以 WPS 为主；这避免将基础能力建立在不可用的 PowerPoint 会话上，同时保留 PowerPoint 的后备兼容性。
- 替代方案：只保留 PowerPoint 会因 COM 会话限制使视觉导出不可用；LibreOffice 仍可作为未来免费后备，但当前未安装且复杂版式兼容性未验证；自行渲染 PPTX 无法可靠还原页面版式。
- 影响：分发包新增 WPS 导出脚本；视觉导出仍为显式选择、只读打开且不执行宏。两个引擎均失败时，Markdown 主流程保持成功并清理失效 `visuals/` 目录。

## D-007：v0.3 采用顺序批处理和可选 Obsidian 输出

- 日期：2026-09-01
- 状态：已接受并已实施
- 决定：批量功能由独立 `BatchConversionService` 顺序调用现有单文件服务；Obsidian 元数据、标签、相对来源链接与原文件复制均作为 `ConversionOptions` 的可选行为，默认关闭。
- 原因：顺序处理可清楚定义输出冲突、文件占用和取消边界，并避免 UI 与适配器混入并发状态。可选输出保持既有 Markdown 协议稳定，防止元数据和原文件复制在个人资料中意外产生隐私或存储影响。
- 替代方案：直接在 UI 中循环转换会使测试、取消和错误恢复不可控；并发批处理会增加资源竞争与冲突处理复杂度；默认启用 Obsidian 元数据或复制原文件会改变现有输出并可能泄露路径或增加冗余。
- 影响：新增领域模型、批处理服务、后台 UI 事件队列和扩展测试；取消仅保证发生于下一项开始前。中文路径名可读性改善必须保持当前安全命名与冲突校验。
- 详细记录：[ADR-004](docs/decisions/ADR-004-batch-and-obsidian-workflow.md)。

## D-012：桌面构建迁移至完整 Tcl/Tk 的独立 Python 3.13 环境

- 日期：2026-09-02
- 状态：已接受，取代 D-008 的构建环境部分。
- 决定：使用项目 `.venv-ui`（Python 3.13.9、Tcl/Tk 8.6）执行桌面应用、测试和 PyInstaller 构建；构建脚本默认指向该环境。
- 原因：既有 `.venv` 基于受控 Python 3.12，真实 Tkinter 窗口创建时无法定位 `init.tcl`。旧 `.venv-tk` 已无法启动；新环境以本机可用 Python 3.13 创建，使用锁定依赖后已通过完整测试、Ruff 和 Tkinter 布局冒烟测试。
- 替代方案：继续使用 `.venv` 会使源码测试可过但桌面程序无法创建窗口；跨版本复用其二进制依赖会导致 `lxml` 导入失败。
- 影响：开发和构建命令应使用 `.venv-ui`；该目录保持 Git 忽略。每次发布仍需执行完整构建及真实窗口启动验证。

## D-013：桌面界面采用单页处理流

- 日期：2026-09-02
- 状态：已接受。
- 决定：重建界面信息架构为“导入文件、输出规则、待处理文件、开始处理”的单页连续流程，不复用此前双栏工作台的布局、色彩或动作区位置。
- 原因：个人归档任务的主路径是加入文件后立即选择输出并执行；将高频操作沿处理顺序组织，可降低视觉跳转，同时让可选 Obsidian 与 PPTX 附件设置保持可见但不抢占主操作。
- 替代方案：保留双栏设置工作台，或引入多页向导。前者未满足重新设计目标，后者会增加批量处理的步骤和上下文切换。
- 影响：仅修改 Tkinter 呈现层和静态布局回归测试；转换服务、后台事件、文件选择和输出安全合同保持不变。

## D-014：使用 Tkinter Canvas 实现指定桌面视觉

- 日期：2026-09-02
- 状态：已接受。
- 决定：保留 Python + Tkinter 技术栈，以 Canvas 自绘方式实现指定的标题栏、圆角卡片、状态胶囊、文件类型徽章和进度条；只将可编辑路径和标签字段嵌入原生 Entry。
- 原因：默认 ttk 控件无法可靠还原给定设计令牌中的圆角、颜色和固定高度，而改用 Electron、WPF 或 WinUI 3 将重写已验证的本地转换与打包链路。
- 影响：UI 层维护自绘命中区、悬停光标与窗口控制；转换逻辑保持无 UI 依赖。发布验证增加 1280x800 的真实窗口布局与交互冒烟检查。

## D-015：使用 Inno Setup 生成当前用户 Windows 安装包

- 日期：2026-09-04
- 状态：已接受并已实施。
- 决定：使用 Inno Setup 6 将 PyInstaller 的目录包封装为单一安装程序；默认安装到 `%LocalAppData%\Programs\廾匸转换`，创建开始菜单入口，提供可选桌面快捷方式和卸载器。
- 原因：目录包需要用户手动保留完整文件夹，不适合正式分发；当前用户安装无需管理员权限，适合本地个人工具，同时完整保留现有 COM/WPS 能力。
- 替代方案：直接分发目录包会导致依赖文件被误删；MSI/WiX 增加维护与构建复杂度；NSIS 在本机未安装且没有额外功能收益。
- 影响：新增安装器脚本和一键构建入口；`release/` 作为可再生产物保持 Git 忽略；每次发布除启动验证外，还需执行安装和卸载冒烟验证。
